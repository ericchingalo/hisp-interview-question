import { TableLayoutPipe } from './table-layout.pipe';

describe('TableLayoutPipe', () => {
  it('create an instance', () => {
    const pipe = new TableLayoutPipe();
    expect(pipe).toBeTruthy();
  });
});
